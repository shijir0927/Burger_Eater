<div class="mt-6">
	<h4 class="devoured-burger-name">{{burger_name}}</h4>

	<button class="delete-burger btn btn-dark" data-id="{{this.id}}"><i class="far fa-trash-alt"></i> Throw away</button>
</div>